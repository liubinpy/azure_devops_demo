trigger:
- none

pool:
  name: my-mac

stages:
- stage: echo
  jobs:
  - job: Echo
    steps:
    - task: Bash@3
      inputs:
        targetType: 'inline'
        script: |          
          echo 'Hello world'
          pwd
          ls -l